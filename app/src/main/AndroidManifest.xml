<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oliverasker.skywarnmarkii">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        >
        <activity android:name=".MainActivity"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".queryLauncher">
        </activity>
        <activity android:name=".UserHomeActivity"></activity>
        <activity android:name=".loginActivity"></activity>
        <activity android:name=".SubmitReportActivity"></activity>
        <activity android:name=".SubmitReportDetails"></activity>
        <activity android:name=".ViewReportActivity"></activity>
        <activity android:name=".ConfirmSubmitReportActivity"></activity>
        <activity android:name=".SearchDBActivity"></activity>
    </application>
</manifest>